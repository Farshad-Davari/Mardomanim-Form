class Validation {
    empty(element) {
        if(element.value == "") {
            element.classList.add("red-border");
            return false;
        }
    }

    // check_int_str(element) {

    // }
}

